<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Remove the temporary database-backed asset records introduced in
         18.0.1.2.0. The module manifest remains the single asset source. -->
    <delete model="ir.asset"
            search="[('path', '=like', 'bambus_hr_daily_ops/static/src/%')]"/>
</odoo>
